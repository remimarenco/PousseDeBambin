@model PousseDeBambin.Models.List

@foreach (var gift in Model.Gifts)
{
    <tr class="fond_cadeaux" id="gift@(gift.GiftId)">
        @Html.Action("DisplayGiftStateTwo", "GiftState", new { id = gift.GiftId })
    </tr>
}
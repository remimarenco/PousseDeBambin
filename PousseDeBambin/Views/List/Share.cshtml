@model PousseDeBambin.Models.List

@{
    ViewBag.Title = "Partage";
}

<h2>Partage de @Model.Name</h2>

<p class="share_list">Bravo ! Vous pouvez partager votre liste "@Model.Name" maintenant !</p>

<p class ="share_list">Donnez tout simplement cette adresse à vos proches : @Html.ActionLink(Url.Action("Manage", "List", new { id = Model.ListId }, Request.Url.Scheme), "Manage", new { id = Model.ListId })</p>